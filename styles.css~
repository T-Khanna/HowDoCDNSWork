/* Move down content because we have a fixed navbar that is 50px tall */
body {
  padding-top: 50px;
  padding-bottom: 20px;
}

#jumbo {
    background-color: transparent;
    position: relative;
    overflow: hidden;
}

#jumbo .container {
    color: #fff;
    text-shadow: 0 0px 5px rgba(0,0,0,.5);
}

video {
    float: left;
    z-index: -100;
    position: absolute;
    top: 0;
    width: 100%;
    height: auto;
}


.flag {
  width: 24px;
  height: 24px;
  overflow: hidden;
  position: relative;
  display: inline-block;
  border-radius: 12px;
  vertical-align: top;
  margin-right: 10px;
}

.flag img {
  position: absolute;
  top: 0;
  left: -6px;
  height: 24px;
}

.profilepic {
  text-align: center;
}

.profilepic img {
  max-width: 50%;
  max-height: 50%;
  border-radius: 50%;
}

.flag, .profilepic img {
  box-shadow: 0 -1px 0 rgba(0,0,0,.2);
}

.social {
  padding-left: 21px;
  list-style-type: none;
}

.social li {
  position: relative;
  line-height: 21px;
}

.social li::before {
  content: '';
  position: absolute;
  width: 16px;
  height: 16px;
  left: -21px;
  top: 2px;
  border-radius: 8px;
}

.social li.li::before {
  background-image: url('assets/img/icons/li.png');
}

.social li.fb::before {
  background-image: url('assets/img/icons/fb.png');
}

.social li.email::before {
  background-image: url('assets/img/icons/mail.png');
}

.social li.icl::before {
  background-image: url('assets/img/icons/icl.png');
}

figure {
  text-align: center;
  margin: 30px 0;
}

figcaption {
  margin-top: 10px;
}

.reference {
  color: inherit !important;
  font-weight: 700;
  text-style: italic;
  padding: 0 2px;
}

ol {
  counter-reset: item;
}

ol > li {
  counter-increment: item;
}

ol ol > li {
  display: block;
}

ol ol {
  padding-left: 0;
}

ol ol > li:before {
  content: counters(item, ".") ". ";
}
